class JsonSpec:
    def init(self, file):
        #load the file
        #check schema macthes our schema e.g. right keys, required keys
        #load the schema using ColumnDefinition, PrimaryKeyDefinition, PermissionDefinition
